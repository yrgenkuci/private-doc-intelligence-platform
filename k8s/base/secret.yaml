apiVersion: v1
kind: Secret
metadata:
  name: doc-intel-secrets
  namespace: doc-intelligence
  labels:
    app.kubernetes.io/name: document-intelligence-platform
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # OPENAI_API_KEY: <base64-encoded-api-key>
  # To create: echo -n "your-api-key" | base64
  # This is a placeholder - should be managed by external secret manager in production
  OPENAI_API_KEY: ""

